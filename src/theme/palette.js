// COLORS

const PRIMARY = {
  main: "#276ef1",
  contrastText: "#fff",
};

const SECONDARY = {
  main: "#fb4c03",
  contrastText: "#fff",
};

const CUSTOM = {
  100: "#102c60",
  200: "#f2f5fd",
};

const palette = {
  common: { black: "#000", white: "#fff" },
  primary: PRIMARY,
  secondary: SECONDARY,
  custom: CUSTOM,
};

export default palette;
